Possible programs (in RDD)

Find all even numbers  to array
sc.range(1, 100)
    .filter(i=>i%2, 0)
    .collect()

Convert text to lowercase
sc.textFile(“textfile.txt”)
    .map(line => line.toLowerCase)

Order random text file of numbers
sc.textFile(“textfile.txt”)
    .soryBy(??)


Need: 
reduce()
reducByKey()
